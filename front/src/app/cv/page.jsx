import CVContent from "./CVContent";

export const metadata = {
  title: "CV - Student Profile",
  description: "Student CV and Professional Details",
};

const CVPage = () => {
  return <CVContent />;
};

export default CVPage;
